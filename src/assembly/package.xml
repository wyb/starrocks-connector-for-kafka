<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.2"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.2
          http://maven.apache.org/xsd/assembly-1.1.2.xsd">
    <!-- Assembles a packaged version targeting OS installation. -->
    <id>package</id>
    <formats>
        <format>dir</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>
    <dependencySets>
        <dependencySet>
            <outputDirectory>share/java/starrocks-kafka-connector</outputDirectory>
            <useProjectArtifact>true</useProjectArtifact>
            <useTransitiveFiltering>true</useTransitiveFiltering>
            <excludes>
                <exclude>io.confluent.*</exclude>
                <exclude>org.apache,kafka.connect.*</exclude>
                <exclude>org.apache.kafka.connect.*</exclude>
                <exclude>org.apache.kafka:connect-api</exclude>
                <exclude>com.starrocks:starrocks-stream-load-sdk</exclude>
                <!-- Shaded into the plugin jar already (see the primary shade execution in
                     pom.xml). Shipping them beside it too puts the same packages in two jars of
                     one plugin directory, and Connect loads every jar there into one classloader,
                     so which copy wins is arbitrary. -->
                <exclude>org.mariadb.jdbc:mariadb-java-client</exclude>
                <exclude>io.debezium:debezium-core</exclude>
                <exclude>org.apache.arrow:flight-sql-jdbc-driver</exclude>
            </excludes>
        </dependencySet>
    </dependencySets>
</assembly>
