# Filtered by Maven at build time (see <resources> in pom.xml). Read by
# com.starrocks.connector.kafka.common.Version so the version a connector
# reports over Connect's REST API is the one that was actually built.
version=${project.version}
